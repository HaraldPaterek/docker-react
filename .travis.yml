sudo: required
services:
  - docker

before_install:
  - docker build -t HaraldPaterek/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true HaraldPaterek/docker-react npm run test

language: generic 